{
  "name": "real-estate-tokenization-platform",
  "version": "1.0.0",
  "description": "Full-stack Real Estate Asset Tokenization Platform for fractional property ownership",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "compile:contracts": "npm run compile --prefix blockchain",
    "deploy:contracts": "npm run deploy --prefix blockchain"
  }
}
